import React from "react";
import "./Footer.scss";

export const Footer: React.FunctionComponent<{}> = (props: {}) => {
  return (
    <footer className="footer bg-white border-top border-primary">
      <div>footer</div>
    </footer>
  );
};
